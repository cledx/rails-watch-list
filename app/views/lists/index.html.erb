<div class="container"><div class="navbar justify-content-between align-items-center">
    <h1>Movie Lists</h1>
    <%= link_to 'New List', new_list_path, class: "btn btn-primary" %>
</div>

<% @lists.each do |list| %>
<ul>
    <li>
        <%= link_to list.name, list_path(list) %>
    </li>
</ul>
<% end %>

</div>